# SPSS Syntax 자동 생성 도구 — 의존성 명세
# 최종 수정: 2026-04-15

# ────────────────────────────────────────────────────────────────
# Python 버전 요구사항
# ────────────────────────────────────────────────────────────────
# Python >= 3.8
#
# 하위 버전 비호환 이유:
#   - f-string (3.6+)
#   - typing.Optional / typing.Dict / typing.List (3.5+, 3.8에서 built-in 대응)
#   - argparse, csv, json, os, pathlib, re, sys, datetime: 모두 표준 라이브러리
#
# 권장: Python 3.9+ (Windows SPSS 환경에서 CP949 처리 안정성)

# ────────────────────────────────────────────────────────────────
# 웹 UI (app.py) 실행 시 필요
# ────────────────────────────────────────────────────────────────
# pip install flask
flask>=2.3

# ────────────────────────────────────────────────────────────────
# CLI 스크립트 (scripts/*.py) — 표준 라이브러리만 사용
# ────────────────────────────────────────────────────────────────
# scripts/ 폴더의 스크립트는 외부 패키지 없이 동작합니다.
# pip install 없이 Python 설치만으로 즉시 실행 가능합니다.
#
# 사용 표준 라이브러리:
#   argparse    CLI 옵션 파싱
#   csv         CSV 파일 읽기
#   json        JSON 설정 파일 파싱
#   os          파일/경로 처리
#   pathlib     Path 객체 처리
#   re          정규식 패턴 매칭
#   sys         프로세스 종료 코드
#   datetime    생성 시각 타임스탬프
#   typing      타입 힌트

# ────────────────────────────────────────────────────────────────
# 개발/테스트 도구 (선택 사항)
# ────────────────────────────────────────────────────────────────
# 아래는 개발 및 테스트 목적으로만 필요합니다.
# 일반 사용 시 설치 불필요.
#
# pytest>=7.0       # 단위 테스트 실행
# black>=23.0       # 코드 포맷터
# flake8>=6.0       # 린터
