# 전국공장등록현황 데이터 정제 파이프라인 업데이트 가이드

> **작성일**: 2026-03-04
> **최종 수정**: 2026-03-07 16:50
> **대상 문서**: `factory_data_cleaning_guide_260305_1657.md` (정제 지침서 v1.5)
> **목적**: 다른 저장소에서 동일한 수정 작업을 재현하기 위한 전체 변경 기록

---

## 1. 버전 이력

| 버전     | 주요 적용 모듈                | 주요 변경                                                                                                                                            | 실행 시각 (YYMMDD_HHMM) |
| -------- | ----------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------- |
| v1.0     | 초기 개발 전반                | 최초 정제 파이프라인 (nid부여, 주소/전화 기초필터링, 법인명칭제거 등 1단계 정규화 및 분리)                                                           | 26/03/04 15:47          |
| v1.1     | `02`, `run`                   | 컬럼 재배치, 읍면동 추출, 원본 컬럼 삭제, `비고`→`회사명_비고`                                                                                       | 26/03/04 17:44          |
| **v1.2** | `04`, `run`                   | **회사명 정규화 영문→한글 외래어 변환, 회사명_영문 전체 로마자+영문 번역 파생 강화**                                                                 | **26/03/04 18:10**      |
| **v1.3** | `05(신규)`, `06`, `run`       | **KSIC 11차 기준 업종 정제 및 대/중분류 매핑, 차수 변환(0/빈칸→11차 가정), 산출물 디렉토리 재구성**                                                  | **26/03/05 09:51**      |
| **v1.4** | `run`                         | **시도-시군구 붙어쓰기 분리, 리포트 비중(%) 추가, 정제 과정별 기초 통계 비교표 자동 산출(5개 소표)**                                                 | **26/03/05 11:17**      |
| **v1.5** | `02`, `run`                   | **시도명 역매핑(SIGUNGU_TO_SIDO), 완전 붙어쓰기 토큰화, 구(기초) 컬럼 분리(11개시), 통계 항목 추가**                                                 | **26/03/07 16:45**      |
| **v1.6** | `02`, `04`, `06`, `07`, `run` | **회사명결함 별도 삭제 처리, 지번 주소 기반 시도명 복사 후처리, 세종특별자치시 단층제 특례, 품질 리포트 자동화 모듈(`07_quality_check`) 통합**       | **26/03/07 17:28**      |
| **v1.7** | `02`, `run`                   | **모든 주소 정제 단계 후에도 시도명/시군구명 추출 불가 업체를 `[삭제:주소결함]` 태깅 후 삭제 리스트로 이동, 통계 `시도시군구_최종미추출` 항목 추가** | **26/03/07 17:50**      |

---

## 2. v1.1 변경 개요

| 항목               | 내용                                                                                                                                                                           |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **컬럼 삭제**      | `공장주소_원본`, `공장주소_지번_원본`                                                                                                                                          |
| **컬럼 이름 변경** | `비고` → `회사명_비고`                                                                                                                                                         |
| **컬럼 위치 변경** | `시도명`/`시군구명` → 공장주소 좌측, `공장주소_클리닝` → 공장주소 우측, `공장주소_지번_클리닝` → 공장주소_지번 우측, `회사명_정규화`/`회사명_영문`/`회사명_비고` → 회사명 우측 |
| **신규 컬럼**      | `읍면동명` (로컬 정규식 기반, 시군구명 우측)                                                                                                                                   |

---

## 3. v1.2 변경 개요

| 항목                 | 내용                                                                         |
| -------------------- | ---------------------------------------------------------------------------- |
| **`회사명_정규화`**  | 영문 외래어(loanword)를 한글로 변환 (예: `Tech` → `테크`, `SJ` → `에스제이`) |
| **`회사명_영문`**    | 정규화된 한글을 전체 영문으로 역변환 (외래어 역변환 + 고유어 로마자 음역)    |
| **신규 매핑 테이블** | `ENGLISH_TO_KOREAN_WORDS` (50+ 항목), `KOREAN_TO_ENGLISH` (80+ 항목)         |
| **신규 함수**        | `english_to_korean()`, `korean_to_english_key()`, `hangul_to_roman()`        |
| **수정 대상**        | `04_clean_company_name.py`, `run_pipeline.py`                                |

---

## 3-1. v1.3 변경 개요

| 항목                | 내용                                                                                          |
| ------------------- | --------------------------------------------------------------------------------------------- |
| **KSIC 업종 정제**  | 원본 `대표업종`(세세분류 5자리)를 KSIC 11차 기준으로 표준화                                   |
| **대/중분류 매핑**  | `대표업종코드(대)`, `대표업종명(대)`, `대표업종코드(중)`, `대표업종명(중)` 4개 컬럼 신규 파생 |
| **차수 변환**       | 10차 코드 → 11차 매핑, 0/빈칸/8/9차는 11차로 가정 처리                                        |
| **업종결함 태그**   | `[삭제:업종결함]` 빈칸/무효코드/변환실패/분류매핑실패 4종                                     |
| **산출물 디렉토리** | `output/factory_on/{YYYYMM}/`, `logs/factory_on/{YYYYMM}/` 구조로 재구성                      |
| **신규 스크립트**   | `05_clean_industry.py`                                                                        |
| **수정 대상**       | `05_clean_industry.py(신규)`, `06_split_deleted.py`, `run_pipeline.py`                        |

---

## 3-2. v1.4 변경 개요

| 항목                 | 내용                                                                                          |
| -------------------- | --------------------------------------------------------------------------------------------- |
| **시도-시군구 분리** | 주소에서 시도명과 시군구명이 붙어쓴 경우 자동 분리 (예: 부산광역시기장군 → 부산광역시 기장군) |
| **리포트 비중(%)**   | 모든 리포트 섹션(처리요약, 삭제사유, 주소, 전화, 기업명, 업종)에 전체 대비 비중(%) 표기 추가  |
| **KSIC 중분류 표기** | 업종코드와 업종명을 병합 표기 (예: `C20 (화학물질 및 화학제품 제조업)`)                       |
| **시도/KSIC 분포표** | 정제비중(%), 삭제비중(%), 전체비중(%) 컬럼 추가                                               |
| **기초 통계 비교표** | 원본/정제/삭제 3자 대조 비교표 5개 소표 자동 산출 (총레코드/주소/전화/기업명/업종)            |
| **수정 대상**        | `02_clean_address.py`, `run_pipeline.py`                                                      |

---

## 3-3. v1.5 변경 개요

| 항목                     | 내용                                                                                         |
| ------------------------ | -------------------------------------------------------------------------------------------- |
| **시도명 역매핑**        | 정제 후 시도명이 빈칸인 경우, 시군구명 기반 `SIGUNGU_TO_SIDO` 사전(약 230건)으로 역매핑 복원 |
| **완전 붙어쓰기 토큰화** | 띄어쓰기 없는 주소(`경기도수원시장안구이목동123`)를 행정구역 경계에서 자동 분리              |
| **구(기초) 컬럼 분리**   | 일반시 산하 기초구(`수원시 장안구`)를 `시군구명`=`수원시` / `구(기초)`=`장안구`로 분리       |
| **대상 시 (11개)**       | 수원시, 성남시, 안양시, 안산시, 고양시, 용인시, 청주시, 천안시, 전주시, 포항시, 창원시       |
| **비대상**               | 특별시/광역시 자치구(강남구, 해운대구 등)는 시군구명에 그대로 유지                           |
| **신규 통계 항목**       | `붙어쓰기_토큰화`, `시도_역매핑`, `구기초_추출`                                              |
| **신규 매핑**            | `SIGUNGU_TO_SIDO`(시군구→시도, ~230건), `CITY_GU_MAP`(일반시→기초구, 11개시)                 |
| **신규 함수**            | `tokenize_full_address()`                                                                    |
| **수정 대상**            | `02_clean_address.py`, `run_pipeline.py`                                                     |

---

## 3-4. v1.6 변경 개요

| 항목                      | 내용                                                                                                                                               |
| ------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| **회사명결함 삭제**       | 특수문자(`-`, `.`, `*` 등)로만 구성되어 정규화 결과가 빈 문자열이 된 회사를 `[삭제:회사명결함]` 태그 처리 후 **삭제 데이터**로 분리                |
| **시도명 보조 복사**      | 시도명이 여전히 빈칸일 경우, 도로명 주소(또는 지번 주소 중 선택되지 않은 것)의 앞부분에서 유효한 시도가 있는지 재검색하여 복원 (`시도_도로명복사`) |
| **세종특별자치시 단층제** | 시도명이 `세종특별자치시`인 경우 시군구명도 자동으로 `세종특별자치시`로 일괄 설정                                                                  |
| **데이터 품질 분석**      | 전체 파이프라인 실행 마지막 단계에 컬럼별 결측수, 빈칸율, 고유값, 숫자 이상치를 점검하는 `07_quality_check.py` 모듈 구동 통합                      |
| **리포트 5번 섹션**       | §5 데이터 품질 분석 (컬럼별 결측/빈칸 현황, 주소 품질, 숫자 이상치 요약) 자동 생성                                                                 |
| **수정 대상**             | `04_clean_company_name.py`, `02_clean_address.py`, `06_split_deleted.py`, `run_pipeline.py`                                                        |
| **신규 대상**             | `07_quality_check.py`                                                                                                                              |

---

## 3-5. v1.7 변경 개요

| 항목                    | 내용                                                                                                                                                                                                          |
| ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **최종 주소 결함 판정** | 모든 정제 단계(오타보정 → 토큰화 → 역매핑 → 도로명보조복사 → 세종특례)를 거친 후에도 `시도명` 또는 `시군구명` 중 하나라도 빈칸인 업체는 `[삭제:주소결함] 시도/시군구 추출불가` 태그 부여 → `deleted.csv` 이동 |
| **기존 태그와 병합**    | 이미 다른 사유로 `[삭제:주소결함]` 태그가 있는 경우 `"                                                                                                                                                        | "` 구분자로 태그를 이어 붙여 사유를 누적 관리 |
| **신규 통계 항목**      | `시도시군구_최종미추출`: 모든 단계 후에도 시도/시군구를 추출하지 못한 건수                                                                                                                                    |
| **수정 대상**           | `02_clean_address.py`, `run_pipeline.py`                                                                                                                                                                      |

---

## 4. 수정 대상 파일 (전체)

```
scripts/
├── 02_clean_address.py       ← v1.1, v1.4, v1.5에서 수정
├── 04_clean_company_name.py  ← v1.1, v1.2에서 수정
├── 05_clean_industry.py      ← v1.3에서 신규 추가
├── 06_split_deleted.py       ← v1.3에서 수정
└── run_pipeline.py           ← v1.1, v1.2, v1.3, v1.4, v1.5에서 수정
```

---

## 5. v1.1 파일별 상세 변경 내역

### 5-1. `02_clean_address.py`

#### 변경 A: 원본 보존 컬럼 생성 코드 삭제

**삭제할 코드** (기존 `clean_addresses()` 함수 내부):
```python
    # 원본 보존 컬럼 생성
    df["공장주소_원본"] = df["공장주소"]
    df["공장주소_지번_원본"] = df["공장주소_지번"]
```

→ 위 3줄을 완전히 삭제합니다.

---

#### 변경 B: 읍면동 추출 패턴 및 함수 추가

`JOINED_SIGUNGU_PATTERNS` 리스트 종료(`]`) 바로 아래, `def normalize_whitespace()` 바로 위에 아래 코드를 삽입합니다:

```python
# ──────────────────────────────────────────
# 읍면동 추출 패턴
# ──────────────────────────────────────────
# 지번 주소에서: {시도} {시군구} [{구}] {읍|면} ...
EUPMYEONDONG_JIBUN_PATTERN = re.compile(
    r'(?:[가-힣]+[시군구]\s+)'
    r'(?:[가-힣]+[구]\s+)?'
    r'([가-힣]+(?:읍|면))\s'
)
# 지번 주소에서 동 추출: {시도} {시군구} [{구}] {동}{N가} ...
DONG_JIBUN_PATTERN = re.compile(
    r'(?:[가-힣]+[시군구]\s+)'
    r'(?:[가-힣]+[구]\s+)?'
    r'([가-힣\d]+동(?:\d+가)?)\s'
)
# 도로명 주소 괄호 내 동명: (xxx동), (xxx동, 건물명)
DONG_PAREN_PATTERN = re.compile(
    r'\(([가-힣\d]+동(?:\d+가)?)(?:,|\))'
)
# 지번 주소에서 리 추출 (읍면 다음): {읍|면} {리} ...
RI_JIBUN_PATTERN = re.compile(
    r'(?:[가-힣]+(?:읍|면)\s+)'
    r'([가-힣]+리)\s'
)


def extract_eupmyeondong(road_addr: str, jibun_addr: str) -> str:
    """
    정제된 주소에서 읍면동명을 추출합니다.
    우선순위: 지번주소(읍면) → 지번주소(동) → 도로명 괄호 내 동명 → 도로명(동)
    Returns: 읍면동명 문자열 (추출 실패 시 빈 문자열)
    """
    # 1차: 지번 주소에서 읍/면 추출
    if jibun_addr:
        m = EUPMYEONDONG_JIBUN_PATTERN.search(jibun_addr)
        if m:
            return m.group(1)
        # 2차: 지번 주소에서 동 추출
        m = DONG_JIBUN_PATTERN.search(jibun_addr)
        if m:
            return m.group(1)

    # 3차: 도로명 주소 괄호 내 동명
    if road_addr:
        m = DONG_PAREN_PATTERN.search(road_addr)
        if m:
            return m.group(1)

    # 4차: 도로명 주소에서도 동 패턴 시도
    if road_addr:
        m = DONG_JIBUN_PATTERN.search(road_addr)
        if m:
            return m.group(1)

    return ""
```

---

#### 변경 C: `clean_addresses()` 함수 끝에 읍면동 추출 로직 추가

`df["_주소태그"] = addr_tags` 바로 아래, `return df, stats` 바로 위에 삽입:

```python
    # 읍면동명 추출
    eupmyeondong_list = []
    for idx in df.index:
        road_cleaned = str(df.at[idx, "공장주소_클리닝"]) if pd.notna(df.at[idx, "공장주소_클리닝"]) else ""
        jibun_cleaned = str(df.at[idx, "공장주소_지번_클리닝"]) if pd.notna(df.at[idx, "공장주소_지번_클리닝"]) else ""
        emd = extract_eupmyeondong(road_cleaned, jibun_cleaned)
        eupmyeondong_list.append(emd)
    df["읍면동명"] = eupmyeondong_list

    stats["읍면동_추출"] = sum(1 for e in eupmyeondong_list if e)
    stats["읍면동_미추출"] = sum(1 for e in eupmyeondong_list if not e)
```

---

#### 변경 D: 테스트 코드 업데이트

`if __name__ == "__main__":` 블록 내 출력 컬럼 리스트 변경:

```diff
-    for col in ["공장주소_원본", "공장주소_클리닝", "공장주소_지번_원본", "공장주소_지번_클리닝", "_주소태그"]:
+    for col in ["공장주소_클리닝", "공장주소_지번_클리닝", "읍면동명", "_주소태그"]:
```

---

### 5-2. `04_clean_company_name.py` (v1.1)

#### 변경 A: 비고 → 회사명_비고 이름 변경

`clean_company_names()` 함수 끝부분:

```diff
-    df["비고"] = notes
+    df["회사명_비고"] = notes
```

#### 변경 B: 테스트 코드 출력문 업데이트

```diff
-        print(f"  {row['회사명']:30s} → 정규화: {row['회사명_정규화']:15s} | 영문: {row['회사명_영문']:15s} | 비고: {row['비고']}")
+        print(f"  {row['회사명']:30s} → 정규화: {row['회사명_정규화']:15s} | 영문: {row['회사명_영문']:15s} | 비고: {row['회사명_비고']}")
```

---

### 5-3. `run_pipeline.py` (v1.1)

#### 변경 A: `step_address()` 출력 보강

함수 끝에 2줄 추가:

```diff
     print(f"       → 주소결함: {addr_stats['주소결함']}건")
+    print(f"       → 읍면동 추출: {addr_stats['읍면동_추출']}건")
+    print(f"       → 읍면동 미추출: {addr_stats['읍면동_미추출']}건")
     return df, addr_stats
```

---

#### 변경 B: `reorder_columns()` 함수 신규 추가

`step_split()` 함수와 `save_results()` 사이에 삽입:

```python
def reorder_columns(df: pd.DataFrame) -> pd.DataFrame:
    """컬럼 순서 재배치"""
    cols = list(df.columns)

    # 이동 대상 컬럼들을 먼저 제거
    move_cols = ["시도명", "시군구명", "읍면동명",
                 "공장주소_클리닝", "공장주소_지번_클리닝",
                 "회사명_정규화", "회사명_영문", "회사명_비고"]
    for mc in move_cols:
        if mc in cols:
            cols.remove(mc)

    # 1. 회사명 우측에 정규화/영문/비고 삽입
    if "회사명" in cols:
        idx = cols.index("회사명") + 1
        for c in ["회사명_정규화", "회사명_영문", "회사명_비고"]:
            if c in df.columns:
                cols.insert(idx, c)
                idx += 1

    # 2. 공장주소 좌측에 시도명/시군구명/읍면동명 삽입
    if "공장주소" in cols:
        idx = cols.index("공장주소")
        for c in ["시도명", "시군구명", "읍면동명"]:
            if c in df.columns:
                cols.insert(idx, c)
                idx += 1

    # 3. 공장주소 우측에 공장주소_클리닝 삽입
    if "공장주소" in cols:
        idx = cols.index("공장주소") + 1
        if "공장주소_클리닝" in df.columns:
            cols.insert(idx, "공장주소_클리닝")

    # 4. 공장주소_지번 우측에 공장주소_지번_클리닝 삽입
    if "공장주소_지번" in cols:
        idx = cols.index("공장주소_지번") + 1
        if "공장주소_지번_클리닝" in df.columns:
            cols.insert(idx, "공장주소_지번_클리닝")

    return df[cols]
```

---

#### 변경 C: `save_results()` 함수에 재배치 호출 추가

```diff
 def save_results(normal_df: pd.DataFrame, deleted_df: pd.DataFrame):
     """정제 결과 CSV 저장"""
+    # 컬럼 재배치
+    normal_df = reorder_columns(normal_df)
+    deleted_df = reorder_columns(deleted_df)
+
     cleaned_path = os.path.join(OUTPUT_DIR, f"factory_cleaned_{TIMESTAMP}.csv")
```

---

#### 변경 D: 리포트 버전 및 내용 업데이트 (v1.0 → v1.1)

```diff
-> **버전**: v1.0
-> **변경 사항**: 최초 정제
+> **버전**: v1.1
+> **변경 사항**: 컬럼 재배치, 읍면동명 추출, 원본 컬럼 삭제, 비고→회사명_비고
```

주소 정제 리포트 섹션:

```diff
 | 수정 건수 합계 | {addr_stats['총_수정']} |
 | 주소결함 건수 | {addr_stats['주소결함']} |
+| 읍면동 추출 | {addr_stats['읍면동_추출']} |
+| 읍면동 미추출 | {addr_stats['읍면동_미추출']} |

 ### 추가된 컬럼
-- `공장주소_원본`: 원본 보존
-- `공장주소_클리닝`: 정제된 도로명 주소
-- `공장주소_지번_원본`: 원본 보존
-- `공장주소_지번_클리닝`: 정제된 지번 주소
+- `공장주소_클리닝`: 정제된 도로명 주소 (공장주소 우측)
+- `공장주소_지번_클리닝`: 정제된 지번 주소 (공장주소_지번 우측)
+- `읍면동명`: 주소 기반 읍면동 추출 (시군구명 우측)
```

기업명 정제 리포트 섹션:

```diff
 ### 추가된 컬럼
-- `회사명_정규화`: 법인 표기/괄호/공백/특수문자 제거, 소문자 통일
-- `회사명_영문`: 영문 부분 추출
-- `비고`: `[중복]`, `[영문포함]` 등 태그
+- `회사명_정규화`: 법인 표기/괄호/공백/특수문자 제거, 소문자 통일 (회사명 우측)
+- `회사명_영문`: 영문 부분 추출 (회사명 우측)
+- `회사명_비고`: `[중복]`, `[영문포함]` 등 태그 (회사명 우측)
```

---

## 6. v1.2 파일별 상세 변경 내역

### 6-1. `04_clean_company_name.py` (v1.2)

#### 변경 A: 영문→한글 외래어 매핑 테이블 추가

모듈 상단 상수 영역에 `ENGLISH_TO_KOREAN_WORDS` 딕셔너리 추가 (50+ 항목):

```python
ENGLISH_TO_KOREAN_WORDS = {
    # 복합 약어 (& 포함)
    "F&B": "에프앤비", "R&D": "알앤디", ...
    # 일반 단어
    "TECH": "테크", "SYSTEM": "시스템", "ENGINEERING": "엔지니어링",
    "PRECISION": "프리시전", "AUTO": "오토", "POWER": "파워", ...
    # 3글자 약어
    "STS": "에스티에스", "MNC": "엠엔씨", ...
    # 2글자 약어
    "OA": "오에이", "SJ": "에스제이", "SK": "에스케이", ...
}
```

#### 변경 B: 알파벳 단위 한글 매핑 (fallback)

매핑 테이블에 없는 영문자를 개별 알파벳 단위로 한글 변환:

```python
_ALPHA_TO_KOREAN = {
    "A": "에이", "B": "비", "C": "씨", "D": "디", ...
}
```

#### 변경 C: 한글→영문 역변환 매핑 테이블 추가

`KOREAN_TO_ENGLISH` 딕셔너리 추가 (80+ 항목, 긴 패턴 우선 매칭):

```python
KOREAN_TO_ENGLISH = {
    "엔지니어링": "engineering", "시스템즈": "systems",
    "프리시전": "precision", "에프앤비": "fnb", ...
    # 알파벳 단위 역매핑
    "더블유": "w", "에이치": "h", "에이": "a", ...
}
```

#### 변경 D: 한글 로마자 음역 함수 추가

국어 로마자 표기법 간소화 기반 `hangul_to_roman()` 함수 추가:

```python
def hangul_to_roman(text: str) -> str:
    """한글 텍스트를 로마자로 음역합니다."""
    # 초성/중성/종성 분해 → 로마자 변환
    ...
```

#### 변경 E: `english_to_korean()` 함수 추가

영문 토큰을 한글 외래어로 변환하는 함수:

```python
def english_to_korean(text: str) -> str:
    """영문 토큰을 한글 외래어로 변환합니다.
    단어/약어 매핑을 먼저 시도하고, 매칭되지 않으면 알파벳 단위로 변환합니다."""
    ...
```

#### 변경 F: `korean_to_english_key()` 함수 추가

정규화된 한글을 전체 영문으로 변환하는 함수:

```python
def korean_to_english_key(text: str) -> str:
    """한글 정규화 텍스트를 전체 영문으로 변환합니다.
    외래어 부분은 영문 역변환, 한글 고유어 부분은 로마자 음역을 적용합니다."""
    ...
```

#### 변경 G: `normalize_company_name()` 로직 변경

기존 `영문 부분 추출` 방식에서 `영문→한글 외래어 변환` 방식으로 변경:

```diff
 # v1.1: 영문 부분을 별도 추출하여 회사명_영문에 저장
 # v1.2: 영문 부분을 한글 외래어로 변환하여 정규화에 포함
-    english_part = re.findall(r'[A-Za-z]+', normalized)
-    ...
+    normalized = english_to_korean(normalized)
```

#### 변경 H: `clean_company_names()` 내 영문 컬럼 생성 로직 변경

```diff
 # v1.1: 영문 부분만 추출
-    df["회사명_영문"] = english_parts
 # v1.2: 정규화된 한글을 전체 영문으로 변환
+    df["회사명_영문"] = df["회사명_정규화"].apply(korean_to_english_key)
```

---

### 6-2. `run_pipeline.py` (v1.2)

#### 변경 A: 리포트 버전 업데이트

```diff
-> **버전**: v1.1
-> **변경 사항**: 컬럼 재배치, 읍면동명 추출, 원본 컬럼 삭제, 비고→회사명_비고
+> **버전**: v1.2
+> **변경 사항**: 회사명 정규화 영문→한글 변환, 회사명 영문 전체 영문화
```

#### 변경 B: 기업명 정제 리포트 컬럼 설명 업데이트

```diff
 ### 추가된 컬럼
-- `회사명_정규화`: 법인 표기/괄호/공백/특수문자 제거, 소문자 통일 (회사명 우측)
-- `회사명_영문`: 영문 부분 추출 (회사명 우측)
+- `회사명_정규화`: 법인표기/괄호 제거, 영문→한글 외래어 변환 (회사명 우측)
+- `회사명_영문`: 정규화 결과의 전체 영문 표현 (외래어 역변환 + 고유어 음역)
 - `회사명_비고`: `[중복]`, `[영문포함]` 등 태그 (회사명 우측)
```

#### 변경 C: `step_company()` 출력에 영문변환 건수 추가

```diff
     print(f"       → 영문포함: {comp_stats['영문_포함']}건")
+    print(f"       → 영문변환: {comp_stats['영문_변환']}건")
     print(f"       → 중복그룹: ...")
```

---

## 7. 최종 컬럼 순서 (v1.5 기준)

```
nid, 관리기관, 회사명, 회사명_정규화, 회사명_영문, 회사명_비고,
공장구분, 단지명, 설립구분, 입주형태, 보유구분,
최초승인일, 최초등록일, 등록구분, 등록일, 전화번호,
남자종업원, 여자종업원, 외국인남자종업원, 외국인여자종업원, 종업원합계,
생산품, 원자재, 공장규모, 용도지역, 지목,
용지면적, 제조시설면적, 부대시설면적, 건축면적,
지식산업센터명,
대표업종코드(대), 대표업종명(대), 대표업종코드(중), 대표업종명(중),  ← [v1.3 신규]
대표업종, 업종명, 업종코드, 차수, 법인주소, 필지수,
시도명, 시군구명, 구(기초), 읍면동명,  ← [구(기초) v1.5 신규]
공장주소, 공장주소_클리닝,
공장주소_지번, 공장주소_지번_클리닝,
공장관리번호
```

---

## 8. 실행 방법

```bash
cd scripts
python -u run_pipeline.py
```

또는 `run.bat` 더블클릭

---

## 9. 실행 후 검증 항목

1. **삭제된 컬럼 확인**: `공장주소_원본`, `공장주소_지번_원본`이 결과 CSV에 없어야 함
2. **이름 변경 확인**: `비고` 대신 `회사명_비고`가 존재해야 함
3. **컬럼 순서 확인**:
   - `회사명` 바로 다음: `회사명_정규화`, `회사명_영문`, `회사명_비고`
   - `공장주소` 바로 앞: `시도명`, `시군구명`, `구(기초)`, `읍면동명`
   - `공장주소` 바로 다음: `공장주소_클리닝`
   - `공장주소_지번` 바로 다음: `공장주소_지번_클리닝`
4. **읍면동 추출률**: 대부분의 행에서 `읍면동명`이 채워져야 함
5. **회사명 정규화 검증**: 영문이 포함된 회사명에서 외래어가 한글로 변환되어야 함
6. **회사명 영문 검증**: 전체 영문 표현이 올바르게 음역/역변환되어야 함
7. **시도명 역매핑 검증**: 시도명이 빈칸인 행 수가 v1.4 대비 감소해야 함
8. **구(기초) 분리 검증**: 수원시/청주시 등 11개시의 기초구가 `구(기초)` 컬럼에 정상 분리되어야 함
9. **자치구 비분리 검증**: 서울 강남구, 부산 해운대구 등 특별시/광역시 자치구는 `구(기초)` 빈칸으로 유지

---

## 10. 실행 결과

### v1.1 실행 (2026-03-04 17:44)

> **소요 시간**: 0.9초

| #   | 검증 항목                                  | 결과                            |
| --- | ------------------------------------------ | ------------------------------- |
| 1   | `공장주소_원본`, `공장주소_지번_원본` 삭제 | ✅ 통과                          |
| 2   | `비고` → `회사명_비고` 변경                | ✅ 통과                          |
| 3   | 컬럼 순서 (§7 기준)                        | ✅ 통과                          |
| 4   | 읍면동 추출률                              | ✅ 99.6% (4,979/5,000)           |
| 5   | 행 수 보존                                 | ✅ 정상 4,282 + 삭제 718 = 5,000 |

| 파일                                     | 건수        |
| ---------------------------------------- | ----------- |
| `output/factory_cleaned_260304_1744.csv` | 4,282행     |
| `output/factory_deleted_260304_1744.csv` | 718행       |
| `logs/cleaning_log_260304_1744.md`       | v1.1 리포트 |

---

### v1.2 실행 (2026-03-04 18:10)

> **소요 시간**: 1.1초

| #   | 검증 항목           | 결과                            |
| --- | ------------------- | ------------------------------- |
| 1   | 컬럼 순서 (§7 기준) | ✅ 통과                          |
| 2   | 읍면동 추출률       | ✅ 99.6% (4,979/5,000)           |
| 3   | 영문 포함 회사명    | 135건 감지                      |
| 4   | 영문→한글 변환      | ✅ 135건 변환 완료               |
| 5   | 행 수 보존          | ✅ 정상 4,282 + 삭제 718 = 5,000 |

| 파일                                     | 건수        |
| ---------------------------------------- | ----------- |
| `output/factory_cleaned_260304_1810.csv` | 4,282행     |
| `output/factory_deleted_260304_1810.csv` | 718행       |
| `logs/cleaning_log_260304_1810.md`       | v1.2 리포트 |

---

### v1.3 실행 (2026-03-05 09:51)

> **소요 시간**: 약 2초

| #   | 검증 항목               | 결과                                                                                   |
| --- | ----------------------- | -------------------------------------------------------------------------------------- |
| 1   | KSIC 대/중분류 4개 컬럼 | ✅ `대표업종코드(대)`, `대표업종명(대)`, `대표업종코드(중)`, `대표업종명(중)` 생성 확인 |
| 2   | 업종결함 태그           | ✅ 빈칸/무효코드/변환실패/분류매핑실패 정상 태깅                                        |
| 3   | 산출물 디렉토리 구조    | ✅ `output/factory_on/`, `logs/factory_on/` 구조 확인                                   |
| 4   | 컬럼 순서 (§7 기준)     | ✅ 통과                                                                                 |

---

### v1.4 실행 (2026-03-05 11:17)

> **소요 시간**: 약 2초

| #   | 검증 항목             | 결과                                            |
| --- | --------------------- | ----------------------------------------------- |
| 1   | 시도-시군구 붙어쓰기  | ✅ 분리 정상 처리                                |
| 2   | 리포트 비중(%) 표기   | ✅ 모든 섹션에 비중 (%) 추가 확인                |
| 3   | 기초 통계 비교표 5개  | ✅ 총레코드/주소/전화/기업명/업종 소표 산출 확인 |
| 4   | KSIC 중분류 병합 표기 | ✅ 코드+업종명 결합 표시 확인                    |

---

### v1.5 실행 (2026-03-07 16:45)

> **소요 시간**: 147.5초

| #   | 검증 항목                 | 결과                                          |
| --- | ------------------------- | --------------------------------------------- |
| 1   | 시도명 역매핑             | ✅ 276,371건 역매핑 성공                       |
| 2   | 시도명 정상/미상          | ✅ 정상 297,524건 / 미상 23건                  |
| 3   | 구(기초) 분리             | ✅ 45,828건 추출                               |
| 4   | 붙어쓰기 보정             | ✅ 1,955건 보정                                |
| 5   | 컬럼 순서 (구(기초) 포함) | ✅ 시도명/시군구명/구(기초)/읍면동명 순서 확인 |
| 6   | 행 수 보존                | ✅ 정상 220,234 + 삭제 77,313 = 297,547        |

| 파일                                                         | 건수        |
| ------------------------------------------------------------ | ----------- |
| `output/factory_on/202601/company_cleaned_260307_1645.csv`   | 220,234행   |
| `output/factory_on/202601/company_deleted_260307_1645.csv`   | 77,313행    |
| `logs/factory_on/202601/company_cleaning_log_260307_1645.md` | v1.5 리포트 |

---

### v1.6 실행 (2026-03-07 17:28)

> **소요 시간**: 153.5초

| #   | 검증 항목             | 결과                                          |
| --- | --------------------- | --------------------------------------------- |
| 1   | 회사명결함 분리       | ✅ 52건 정상적으로 Deleted CSV로 이동 완료     |
| 2   | 시도명 빈칸 감소      | ✅ v1.5 기준 5건 → v1.6 기준 1건으로 크게 개선 |
| 3   | 품질 분석 리포트 통합 | ✅ 리포트 §5 데이터 품질 분석 (컬럼 요약 포함) |
| 4   | 행 수 보존            | ✅ 정상 220,182 + 삭제 77,365 = 297,547        |

| 파일                                                         | 건수        |
| ------------------------------------------------------------ | ----------- |
| `output/factory_on/202601/company_cleaned_260307_1725.csv`   | 220,182행   |
| `output/factory_on/202601/company_deleted_260307_1725.csv`   | 77,365행    |
| `logs/factory_on/202601/company_cleaning_log_260307_1725.md` | v1.6 리포트 |
