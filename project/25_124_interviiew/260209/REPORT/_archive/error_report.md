# 실행 에러 리포트

## 발생일시: 2026-02-09 18:25

---

## 에러 1: Python 터미널 초기화 지연

### 현상
- `01_preprocess.py` 스크립트 실행 시 Python 프로세스가 30초 이상 응답 없음
- cmd 및 powershell 두 가지 방식 모두 동일한 지연 발생
- 이전 대화 세션에서도 동일한 문제 반복 확인됨

### 원인 분석
- Windows 환경에서 Python 인터프리터 초기화 단계의 지연 (라이브러리 로딩, 환경 변수 검색 등)
- 터미널 에이전트의 Python 프로세스 관리 이슈 가능성

### 추가 진단 (2026-02-09 20:31)

**테스트 수행:**
1. `python --version` → 10초 대기 후 출력 없음, RUNNING 상태 유지
2. `where python` → 동일하게 행 걸림
3. `echo test` → **Python과 무관한 기본 명령도 행 걸림**

**진단 결과:**
- Python 고유의 문제가 **아님** — cmd 기본 명령(`echo`)도 동일 증상
- **터미널 에이전트의 cmd 셸 출력 캡처 이슈**가 근본 원인
- 명령은 셸에 전달되나, 출력 스트림(stdout)이 에이전트로 반환되지 않음
- 모든 명령이 Background 전환 후 RUNNING 상태로 무한 대기

**결론:** 이 환경에서 터미널 명령 실행은 신뢰할 수 없으며, **수동 분석 또는 파일 도구(view_file, write_to_file 등)만 사용**하는 것이 안정적임

### 조치
- Python 스크립트 실행을 포기하고 **수동 분석 방식으로 전환**
- CSV 원본 데이터를 직접 읽어 수동으로 텍스트 코딩 및 교차분석 작성
- `preprocessed.csv`와 `coding_table.csv` 생성은 스킵됨 (보고서에는 영향 없음)

### 영향도
- **보고서 품질**: 영향 없음 (데이터 직접 확인 후 분석 수행)
- **재현성**: 향후 스크립트 실행 시 동일 환경에서 재발 가능

### 해결 방안 (향후)
1. Python 스크립트 실행 전 `python --version` 등 최소 명령으로 환경 사전 점검
2. 스크립트를 여러 작은 단위로 분할 실행
3. 가능하다면 Jupyter Notebook 환경 또는 별도 터미널에서 실행

---

## 에러 2: 해당 없음

본 분석에서 위 Python 실행 지연 외 추가 에러는 발생하지 않았음

---

## 변경사항 추적 (2026-02-09 20:00~20:30)

### 변경 1: 데이터 오류 정정

| 항목             | 기존(개별보고서) | 수정(통합보고서) | 비고                   |
| ---------------- | ---------------- | ---------------- | ---------------------- |
| 제조업 기업 수   | 14개             | **15개**         | 한텍(가스-제조업) 누락 |
| 30~50명 기업 수  | 8개              | **9개**          | 대송컨테이너항업 포함  |
| 제조업 비율 분모 | /14              | **/15**          | 전체 테이블 재계산     |

- `05_summary_wording.md`: 제조업(14개)→**(15개)** 수정
- `05_summary_wording.txt`: 제조업(14개)→**(15개)** 수정

### 변경 2: 통합 워딩 파일 재작성

기존 통합 워딩(`00_integrated_wording.txt`, `00_integrated_wording.md`)이 축약/불릿 형태로 작성되어 개별 워딩과 워딩이 달랐음. 개별 워딩 6개 파일(A~D, Q_ETC, 종합분석)의 원문 서술형 문장을 그대로 취합하여 재작성.

**수정 파일:**
- `00_integrated_wording.txt` — 전면 재작성
- `00_integrated_wording.md` — 전면 재작성

### 변경 3: 통합 리포트 업종별 테이블 누락 보완

기존 통합 리포트(`00_integrated_report.md`)에서 일부 하위 문항의 업종별 테이블이 한 줄 요약으로만 기재되어 있었음. 개별 보고서에 있는 업종별 테이블을 모두 추가.

**추가된 테이블:**

| 섹션                             | 추가 내용                                       |
| -------------------------------- | ----------------------------------------------- |
| B-2. 강사 변경 영향 (Q3_1)       | 업종별 테이블 추가 (전체/제조/건설/서비스/기타) |
| B-3. 최신동향 재수강 의향 (Q3_2) | 업종별 테이블 추가                              |
| D-2. 선호·우려 (Q7_1)            | 업종별 테이블 추가 (보상·인센티브 항목 포함)    |
| D-3. 대안 방식 (Q7_2)            | 업종별 테이블 추가 (선택적 운영 항목 포함)      |

- 모든 추가 테이블의 제조업 비율은 /15 기준으로 재계산 반영
- 기업 수 합계가 업종 합계(15+2+1+3=21)와 일치 검증 완료
