# OpenAPI 활용 웹 애플리케이션 개발 가이드라인

## 📋 목차
1. [개요](#개요)
2. [개발 환경 구성](#개발-환경-구성)
3. [프로젝트 구조](#프로젝트-구조)
4. [핵심 구현 패턴](#핵심-구현-패턴)
5. [API 연동 가이드](#api-연동-가이드)
6. [UI/UX 디자인 가이드](#uiux-디자인-가이드)
7. [에러 처리](#에러-처리)
8. [테스트 및 검증](#테스트-및-검증)
9. [배포 및 유지보수](#배포-및-유지보수)

---

## 개요

이 문서는 공공데이터포털의 OpenAPI를 활용한 웹 애플리케이션 개발 시 참고할 수 있는 가이드라인입니다. 병원정보조회 웹 애플리케이션을 예시로 하여 일반적인 OpenAPI 웹 앱 개발 방법을 설명합니다.

### 적용 대상
- 공공데이터포털 OpenAPI를 활용한 웹 애플리케이션
- REST API 기반 데이터 조회 및 다운로드 서비스
- 단일 HTML 파일 형태의 경량 웹 애플리케이션

### 주요 특징
- ✅ 서버 없이 클라이언트 사이드만으로 동작
- ✅ 단일 HTML 파일로 배포 및 사용 간편
- ✅ 외부 라이브러리 CDN 활용으로 설치 불필요
- ✅ 반응형 디자인으로 다양한 디바이스 지원

---

## 개발 환경 구성

### 필수 도구
1. **텍스트 에디터**: VS Code, Sublime Text, Notepad++ 등
2. **웹 브라우저**: Chrome, Edge, Firefox (개발자 도구 포함)
3. **인터넷 연결**: CDN 라이브러리 및 API 호출용

### 권장 VS Code 확장
- Live Server: 로컬 개발 서버
- Prettier: 코드 포맷팅
- ESLint: JavaScript 린팅

### 외부 라이브러리 (CDN)

#### 필수 라이브러리
```html
<!-- Bootstrap: UI 프레임워크 -->
<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css">

<!-- SheetJS: 엑셀 파일 생성 -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/xlsx/0.17.0/xlsx.core.min.js"></script>

<!-- FileSaver.js: 파일 다운로드 -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/FileSaver.js/1.3.8/FileSaver.min.js"></script>
```

#### 선택 라이브러리
```html
<!-- jQuery (필요시) -->
<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>

<!-- Axios (fetch 대신 사용 가능) -->
<script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
```

---

## 프로젝트 구조

### 단일 HTML 파일 구조

```
웹애플리케이션.html
├── <head>
│   ├── 메타 태그 (charset, viewport)
│   ├── 타이틀
│   ├── 외부 CSS 링크 (Bootstrap 등)
│   ├── 외부 JS 링크 (라이브러리)
│   └── <style> 커스텀 CSS
└── <body>
    ├── 컨테이너
    │   ├── 헤더 섹션
    │   ├── API 인증키 입력 섹션
    │   ├── 검색 조건 입력 섹션
    │   ├── 결과 표시 섹션
    │   └── 주의사항 섹션
    └── <script> JavaScript 로직
        ├── 전역 변수
        ├── 코드 데이터 (상수)
        ├── 초기화 함수
        ├── API 호출 함수
        ├── 결과 처리 함수
        ├── UI 업데이트 함수
        └── 유틸리티 함수
```

### 폴더 구조 (프로젝트 전체)

```
프로젝트_폴더/
├── 웹애플리케이션.html          # 메인 애플리케이션
├── README.md                    # 사용 가이드
├── 개발_가이드라인.md            # 이 문서
├── API_가이드라인.md             # API 명세 및 사용법
├── REPORT/                      # 개발 과정 문서
│   ├── 구현_상세_문서_YYYYMMDD_HHMMSS.md
│   ├── 테스트_결과_YYYYMMDD_HHMMSS.md
│   └── 개발_일지_YYYYMMDD_HHMMSS.md
└── docs/                        # 참고 문서
    └── 원본_API_가이드.md
```

---

## 핵심 구현 패턴

### 1. HTML 구조 패턴

#### 섹션 카드 패턴
모든 주요 기능을 독립적인 카드 형태로 구성:

```html
<div class="section-card">
    <div class="section-title">섹션 제목</div>
    <div class="section-content">
        <!-- 섹션 내용 -->
    </div>
</div>
```

**장점:**
- 시각적으로 구분이 명확
- 반응형 디자인 적용 용이
- 유지보수 편리

#### 폼 그리드 패턴
검색 조건 등 여러 입력 필드를 그리드로 배치:

```html
<div class="search-grid">
    <div class="form-group">
        <label>필드명</label>
        <input type="text" class="form-control">
    </div>
    <!-- 반복 -->
</div>
```

```css
.search-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 20px;
}
```

**장점:**
- 자동으로 반응형 레이아웃
- 일관된 간격 유지
- 유연한 컬럼 수 조정

### 2. JavaScript 구조 패턴

#### 전역 변수 관리
```javascript
// 상태 관리용 전역 변수
let currentData = [];      // 현재 검색 결과
let currentPage = 1;       // 현재 페이지
let totalCount = 0;        // 총 결과 수
let numOfRows = 50;        // 페이지당 결과 수

// 코드 데이터 (상수)
const API_BASE_URL = 'https://api.example.com';
const CODE_DATA = {
    // 코드 매핑 데이터
};
```

#### 비동기 함수 패턴
```javascript
async function fetchData(params) {
    try {
        // API 호출
        const response = await fetch(url);
        
        if (!response.ok) {
            throw new Error(`HTTP Error: ${response.status}`);
        }
        
        const data = await response.json();
        
        // 데이터 처리
        handleResponse(data);
        
    } catch (error) {
        // 에러 처리
        handleError(error);
    }
}
```

#### 함수 명명 규칙
```javascript
// 조회/가져오기: get, fetch
async function getHospitalList() { }

// 구성/생성: build, create
function buildSearchParams() { }

// 처리: handle, process
function handleApiResponse(data) { }

// 표시/업데이트: display, update, render
function displayResults() { }
function updatePagination() { }

// 검증: validate, check
function validateInput() { }

// 유틸리티: format, convert
function formatDate(dateStr) { }
```

### 3. CSS 스타일링 패턴

#### 그라디언트 활용
```css
/* 헤더 그라디언트 */
.header {
    background: linear-gradient(135deg, #003668 0%, #004d99 100%);
}

/* 버튼 그라디언트 */
.btn-primary {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
}
```

#### 호버 효과
```css
.section-card {
    transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.section-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.15);
}
```

#### 애니메이션
```css
@keyframes slideDown {
    from {
        opacity: 0;
        transform: translateY(-20px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

.header {
    animation: slideDown 0.5s ease-out;
}
```

---

## API 연동 가이드

### 1. API 호출 기본 패턴

#### Fetch API 사용
```javascript
async function callApi(params) {
    const BASE_URL = 'https://api.example.com/endpoint';
    const queryString = new URLSearchParams(params).toString();
    const url = `${BASE_URL}?${queryString}`;
    
    try {
        const response = await fetch(url);
        
        if (!response.ok) {
            throw new Error(`HTTP ${response.status}`);
        }
        
        const data = await response.json();
        return data;
        
    } catch (error) {
        console.error('API Error:', error);
        throw error;
    }
}
```

### 2. 파라미터 처리

#### 필수 파라미터
```javascript
const params = {
    ServiceKey: apiKey,    // 인증키 (필수)
    pageNo: 1,             // 페이지 번호
    numOfRows: 10,         // 결과 수
    _type: 'json'          // 응답 형식
};
```

#### 선택 파라미터 추가
```javascript
function buildParams() {
    const params = {
        ServiceKey: getApiKey(),
        pageNo: currentPage,
        numOfRows: numOfRows,
        _type: 'json'
    };
    
    // 선택 파라미터 (값이 있을 때만 추가)
    const optionalParams = {
        sidoCd: document.getElementById('sidoCd').value,
        yadmNm: document.getElementById('yadmNm').value.trim()
        // ...
    };
    
    for (const [key, value] of Object.entries(optionalParams)) {
        if (value) {
            params[key] = value;
        }
    }
    
    return params;
}
```

### 3. 응답 처리

#### 표준 응답 구조
```javascript
{
    "response": {
        "header": {
            "resultCode": "00",
            "resultMsg": "NORMAL SERVICE."
        },
        "body": {
            "numOfRows": 10,
            "pageNo": 1,
            "totalCount": 100,
            "items": {
                "item": [
                    { /* 데이터 */ },
                    { /* 데이터 */ }
                ]
            }
        }
    }
}
```

#### 응답 처리 함수
```javascript
function handleApiResponse(data) {
    // 헤더 확인
    const header = data.response.header;
    if (header.resultCode !== '00') {
        handleApiError(header.resultCode, header.resultMsg);
        return;
    }
    
    // 바디 확인
    const body = data.response.body;
    const totalCount = body.totalCount || 0;
    
    if (totalCount === 0) {
        alert('검색 결과가 없습니다.');
        return;
    }
    
    // 아이템 처리 (단일/복수 처리)
    let items = body.items?.item || [];
    if (!Array.isArray(items)) {
        items = [items];  // 단일 결과를 배열로 변환
    }
    
    // 데이터 저장 및 표시
    currentData = items;
    displayResults();
}
```

### 4. 에러 처리

#### API 에러 코드 매핑
```javascript
const API_ERROR_MESSAGES = {
    '0': '정상',
    '3': '검색 결과가 없습니다.',
    '22': '일일 트래픽 제한을 초과했습니다.',
    '30': '등록되지 않은 인증키입니다.',
    '31': '기한이 만료된 인증키입니다.'
};

function handleApiError(errorCode, errorMsg) {
    const message = API_ERROR_MESSAGES[errorCode] || errorMsg;
    alert(`API 오류 [${errorCode}]: ${message}`);
}
```

---

## UI/UX 디자인 가이드

### 1. 색상 팔레트

#### 기본 색상
```css
:root {
    --primary-color: #667eea;
    --primary-dark: #764ba2;
    --secondary-color: #11998e;
    --secondary-light: #38ef7d;
    --dark-blue: #003668;
    --dark-blue-light: #004d99;
    --text-dark: #333;
    --text-light: #666;
    --bg-light: #f8f9ff;
    --border-color: #e0e0e0;
}
```

#### 사용 예시
```css
/* 주요 버튼 */
.btn-primary {
    background: linear-gradient(135deg, var(--primary-color) 0%, var(--primary-dark) 100%);
}

/* 성공 버튼 */
.btn-success {
    background: linear-gradient(135deg, var(--secondary-color) 0%, var(--secondary-light) 100%);
}
```

### 2. 타이포그래피

```css
body {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    font-size: 16px;
    line-height: 1.6;
    color: var(--text-dark);
}

h1 { font-size: 2rem; font-weight: bold; }
h2 { font-size: 1.5rem; font-weight: 600; }
h3 { font-size: 1.3rem; font-weight: 600; }
```

### 3. 간격 시스템

```css
/* 일관된 간격 사용 */
.spacing-xs { margin: 5px; }
.spacing-sm { margin: 10px; }
.spacing-md { margin: 15px; }
.spacing-lg { margin: 20px; }
.spacing-xl { margin: 30px; }
```

### 4. 반응형 디자인

#### 브레이크포인트
```css
/* 모바일 우선 접근 */
/* 기본: 모바일 (< 768px) */

/* 태블릿 */
@media (min-width: 768px) {
    .search-grid {
        grid-template-columns: repeat(2, 1fr);
    }
}

/* 데스크톱 */
@media (min-width: 1024px) {
    .search-grid {
        grid-template-columns: repeat(3, 1fr);
    }
}

/* 대형 화면 */
@media (min-width: 1400px) {
    .container {
        max-width: 1400px;
    }
}
```

### 5. 로딩 인디케이터

```html
<div class="loading" id="loadingIndicator">
    <div class="spinner"></div>
    <p>데이터를 불러오는 중...</p>
</div>
```

```css
.spinner {
    border: 4px solid #f3f3f3;
    border-top: 4px solid #667eea;
    border-radius: 50%;
    width: 40px;
    height: 40px;
    animation: spin 1s linear infinite;
    margin: 0 auto;
}

@keyframes spin {
    0% { transform: rotate(0deg); }
    100% { transform: rotate(360deg); }
}
```

```javascript
function showLoading() {
    document.getElementById('loadingIndicator').classList.add('active');
}

function hideLoading() {
    document.getElementById('loadingIndicator').classList.remove('active');
}
```

---

## 에러 처리

### 1. 입력 검증

```javascript
function validateInput() {
    const apiKey = document.getElementById('apiKey').value.trim();
    
    if (!apiKey) {
        alert('API 인증키를 입력해 주세요.');
        document.getElementById('apiKey').focus();
        return false;
    }
    
    return true;
}
```

### 2. API 에러 처리

```javascript
async function safeApiCall(params) {
    try {
        showLoading();
        
        const data = await callApi(params);
        
        // 응답 헤더 확인
        if (data.response.header.resultCode !== '00') {
            throw new ApiError(
                data.response.header.resultCode,
                data.response.header.resultMsg
            );
        }
        
        return data;
        
    } catch (error) {
        if (error instanceof ApiError) {
            handleApiError(error.code, error.message);
        } else {
            handleNetworkError(error);
        }
        return null;
        
    } finally {
        hideLoading();
    }
}
```

### 3. 네트워크 에러 처리

```javascript
function handleNetworkError(error) {
    console.error('Network Error:', error);
    
    let message = 'API 호출 중 오류가 발생했습니다.\n';
    
    if (error.name === 'TypeError') {
        message += '네트워크 연결을 확인해 주세요.';
    } else if (error.message.includes('timeout')) {
        message += '요청 시간이 초과되었습니다. 잠시 후 다시 시도해 주세요.';
    } else {
        message += '잠시 후 다시 시도해 주세요.';
    }
    
    alert(message);
}
```

---

## 테스트 및 검증

### 1. 기능 테스트 체크리스트

#### API 연동
- [ ] 인증키 입력 없이 검색 시 경고 표시
- [ ] 올바른 인증키로 검색 시 결과 표시
- [ ] 잘못된 인증키로 검색 시 에러 메시지
- [ ] 검색 결과 없을 때 메시지 표시

#### 검색 기능
- [ ] 각 검색 조건 개별 테스트
- [ ] 검색 조건 조합 테스트
- [ ] 검색 결과 정확성 확인

#### 페이징
- [ ] 페이지 번호 클릭 시 해당 페이지 로드
- [ ] 이전/다음 버튼 동작
- [ ] 첫 페이지에서 이전 버튼 비활성화
- [ ] 마지막 페이지에서 다음 버튼 비활성화

#### 엑셀 다운로드
- [ ] 검색 결과를 엑셀로 다운로드
- [ ] 파일명 형식 확인
- [ ] 엑셀 파일 내용 확인
- [ ] 한글 깨짐 없음 확인

### 2. 브라우저 호환성 테스트

| 브라우저 | 버전 | 테스트 항목 |
|---------|------|-----------|
| Chrome | 최신 | 모든 기능 |
| Edge | 최신 | 모든 기능 |
| Firefox | 최신 | 모든 기능 |
| Safari | 최신 | 모든 기능 |

### 3. 반응형 테스트

| 디바이스 | 해상도 | 테스트 항목 |
|---------|--------|-----------|
| 모바일 | 320px ~ 767px | 레이아웃, 터치 동작 |
| 태블릿 | 768px ~ 1023px | 레이아웃, 터치 동작 |
| 데스크톱 | 1024px 이상 | 전체 기능 |

---

## 배포 및 유지보수

### 1. 배포 방법

#### 방법 1: 파일 공유
1. HTML 파일을 사용자에게 전달
2. 사용자가 브라우저로 파일 열기
3. 인터넷 연결 필요 (CDN 라이브러리 및 API 호출)

#### 방법 2: 웹 서버 호스팅
1. HTML 파일을 웹 서버에 업로드
2. URL로 접근 가능
3. 예: `https://example.com/hospital-search.html`

#### 방법 3: GitHub Pages
1. GitHub 저장소 생성
2. HTML 파일 업로드
3. Settings > Pages에서 활성화
4. `https://username.github.io/repo-name/` 형태로 접근

### 2. 버전 관리

```html
<!-- HTML 파일 상단에 버전 정보 주석 -->
<!--
    애플리케이션명: 병원정보조회
    버전: 1.0.0
    최종 수정일: 2026-01-13
    변경 내역:
    - 1.0.0 (2026-01-13): 초기 버전
-->
```

### 3. 유지보수 가이드

#### 정기 점검 항목
- [ ] API 엔드포인트 변경 확인
- [ ] 외부 라이브러리 CDN 링크 유효성 확인
- [ ] 브라우저 호환성 확인
- [ ] 코드 데이터 (시도/시군구 등) 업데이트

#### 문제 발생 시 확인 사항
1. 브라우저 콘솔에서 에러 메시지 확인
2. 네트워크 탭에서 API 호출 상태 확인
3. API 인증키 유효성 확인
4. 외부 라이브러리 로드 확인

---

## 부록

### A. 유용한 코드 스니펫

#### 날짜 포맷팅
```javascript
function formatDate(dateStr, format = 'YYYY-MM-DD') {
    if (!dateStr || dateStr.length !== 8) return '-';
    
    const year = dateStr.substring(0, 4);
    const month = dateStr.substring(4, 6);
    const day = dateStr.substring(6, 8);
    
    return format
        .replace('YYYY', year)
        .replace('MM', month)
        .replace('DD', day);
}
```

#### 숫자 포맷팅 (천 단위 구분)
```javascript
function formatNumber(num) {
    return num.toLocaleString('ko-KR');
}
```

#### 디바운싱 (검색 입력 최적화)
```javascript
function debounce(func, wait) {
    let timeout;
    return function executedFunction(...args) {
        const later = () => {
            clearTimeout(timeout);
            func(...args);
        };
        clearTimeout(timeout);
        timeout = setTimeout(later, wait);
    };
}

// 사용 예시
const searchInput = document.getElementById('searchInput');
searchInput.addEventListener('input', debounce(function(e) {
    performSearch(e.target.value);
}, 300));
```

### B. 참고 자료

- [공공데이터포털](https://www.data.go.kr)
- [MDN Web Docs](https://developer.mozilla.org)
- [Bootstrap 문서](https://getbootstrap.com/docs/4.1)
- [SheetJS 문서](https://docs.sheetjs.com)

---

## 버전 정보
- **문서 버전**: 1.0
- **최종 수정일**: 2026-01-13
- **작성자**: OpenAPI 웹 애플리케이션 개발팀
